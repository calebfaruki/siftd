import { DarkTheme } from '@react-navigation/native';

const SiftdTheme = {
  ...DarkTheme,
  colors: {
    ...DarkTheme.colors,
    primary: '#FFF',
    text: '#FFFFFF',
  },
};

export default SiftdTheme;